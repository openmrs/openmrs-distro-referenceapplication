# This file is intended as an override to build the backend using the local m2 repo
version: "3.7"

services:
  gateway:
    build:
      context: ./gateway

  frontend:
    build:
      context: ./frontend

  backend:
    build:
      context: .
    volumes:
      - ~/.m2:/root/.m2
