# version: "3.7"

services:
  certbot:
    build:
      context: ./certbot

  gateway:
    build:
      context: ./gateway

  frontend:
    build:
      context: ./frontend

  backend:
    build:
      context: .
